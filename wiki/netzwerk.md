# Netzwerk

## Wichtige Befehle
- `ping example.com` testet die Erreichbarkeit.
- `ip a` zeigt Netzwerkschnittstellen.
- `netstat -an` zeigt Netzwerkverbindungen.

## Hinweis
Je nach Umgebung sind manche Befehle unter Git Bash anders oder nicht verfügbar.