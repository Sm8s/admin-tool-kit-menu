# Prozesse

## Wichtige Befehle
- `ps` zeigt laufende Prozesse.
- `top` zeigt Prozesse in Echtzeit.
- `kill PID` beendet einen Prozess.

## Hinweis
Unter Windows mit Git Bash können Ausgaben von echtem Linux abweichen.